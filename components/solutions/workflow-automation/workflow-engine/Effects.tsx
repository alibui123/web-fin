"use client"

/**
 * Post layer placeholder.
 * Rectangular bloom planes were removed — they read as a teal box behind the pipeline.
 * Soft glow now lives only on modules / packets (additive spheres).
 */
export default function Effects(_props: { enabled: boolean }) {
  return null
}
