"use client"

/**
 * Soft post layer.
 * Intentionally no rectangular bloom planes — those read as teal UI boxes
 * on dark Finova pages (seen on the workflow engine).
 * Glow lives on module emissives + circular halos only.
 */
export default function Effects(_props: { enabled: boolean }) {
  return null
}
